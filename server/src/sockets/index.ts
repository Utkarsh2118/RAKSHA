/**
 * Socket.IO will be initialized from this module in a later phase.
 * This file exists so the realtime layer has a stable import path.
 */
export function initSockets(_httpServer: unknown): void {
  return;
}
